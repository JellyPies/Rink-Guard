import { RentalPictoPipe } from './rental-picto.pipe';

describe('RentalPictoPipe', () => {
  it('create an instance', () => {
    const pipe = new RentalPictoPipe();
    expect(pipe).toBeTruthy();
  });
});
